#include <stdio.h>
int main()
{
    int arr[100], i;
    printf("Enter size of the array you want to sort:\n");
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        printf("Enter the element of the array:\n");
        scanf("%d", &arr[i]);
        /* code */
    }

    return 0;
}